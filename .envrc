# Automatic shell activation with direnv (`direnv allow`).
# This avoids pinning a devenv direnvrc hash: it just evaluates the dev env.
watch_file devenv.nix devenv.yaml
eval "$(devenv print-dev-env)"
